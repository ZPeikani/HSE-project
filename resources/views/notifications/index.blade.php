@extends('layouts.app') @section('title','اعلان‌ها') @section('page-title','اعلان‌ها و سررسیدها') @section('content')
<div class="mx-auto max-w-4xl overflow-hidden rounded-2xl border bg-white"><div class="border-b p-5"><h2 class="font-black">مرکز اعلان HSE</h2><p class="text-xs text-slate-400">یادآوری قبل از سررسید، روز سررسید و موارد معوق</p></div><div class="divide-y">@forelse($notifications as $n)<form method="post" action="{{ route('notifications.read',$n) }}" class="flex items-center gap-4 p-5 {{ $n->read_at?'opacity-60':'bg-amber-50/40' }}">@csrf<div class="h-3 w-3 rounded-full {{ $n->type==='overdue'?'bg-rose-500':($n->type==='due_today'?'bg-amber-500':'bg-sky-500') }}"></div><button class="min-w-0 flex-1 text-right"><div class="font-bold">{{ $n->title }}</div><div class="mt-1 text-sm text-slate-500">{{ $n->message }}</div></button><span class="text-xs text-slate-400">@jdate($n->due_at)</span></form>@empty<p class="p-12 text-center text-slate-400">اعلان فعالی وجود ندارد.</p>@endforelse</div><div class="border-t p-4">{{ $notifications->links() }}</div></div>@endsection
