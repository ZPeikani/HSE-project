@extends('layouts.app') @section('title','درخواست PTW') @section('page-title','درخواست مجوز کار') @section('content')
<form method="post" action="{{ route('permits.store') }}" class="mx-auto max-w-5xl rounded-2xl border bg-white p-6">@csrf<div class="grid gap-5 md:grid-cols-2">@include('components.field',['label'=>'عنوان کار','name'=>'title'])<label><span class="mb-2 block text-sm font-bold">نوع مجوز *</span><select name="type" class="w-full rounded-xl border bg-slate-50 p-3">@foreach(['hot_work'=>'کار گرم','confined_space'=>'فضای بسته','electrical'=>'برق','height'=>'کار در ارتفاع','excavation'=>'حفاری','lifting'=>'لیفتینگ','general'=>'عمومی'] as $k=>$v)<option value="{{ $k }}">{{ $v }}</option>@endforeach</select></label><label><span class="mb-2 block text-sm font-bold">واحد *</span><select name="department_id" class="w-full rounded-xl border bg-slate-50 p-3">@foreach($departments as $d)<option value="{{ $d->id }}">{{ $d->name }}</option>@endforeach</select></label>@include('components.field',['label'=>'محل اجرا','name'=>'location']) @include('components.field',['label'=>'شروع','name'=>'starts_at','type'=>'datetime-local']) @include('components.field',['label'=>'پایان','name'=>'ends_at','type'=>'datetime-local'])@foreach(['description'=>'شرح کار','hazards'=>'خطرات شناسایی‌شده','controls'=>'کنترل‌ها و پیش‌نیازهای ایمنی'] as $name=>$label)<label class="md:col-span-2"><span class="mb-2 block text-sm font-bold">{{ $label }} *</span><textarea name="{{ $name }}" required rows="3" class="w-full rounded-xl border bg-slate-50 p-4">{{ old($name) }}</textarea>@error($name)<p class="mt-1 text-xs text-rose-600">{{ $message }}</p>@enderror</label>@endforeach</div><div class="mt-6 flex justify-end"><button class="rounded-xl bg-emerald-600 px-7 py-3 font-black text-white">ثبت درخواست PTW</button></div></form>@endsection
