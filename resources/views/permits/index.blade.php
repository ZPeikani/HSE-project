@extends('layouts.app') @section('title','PTW') @section('page-title','مجوزهای کار PTW') @section('content')
<div class="mb-5 flex flex-col justify-between gap-3 sm:flex-row"><div><h2 class="text-xl font-black">کنترل مجوزهای کار پرخطر</h2><p class="text-sm text-slate-500">درخواست، ارزیابی، کنترل، تأیید، اجرا و بستن</p></div><a href="{{ route('permits.create') }}" class="rounded-xl bg-emerald-600 px-5 py-3 text-center text-sm font-bold text-white">+ درخواست مجوز</a></div><form class="mb-4 flex gap-2"><input name="q" value="{{ request('q') }}" placeholder="کد یا عنوان مجوز" class="min-w-0 flex-1 rounded-xl border bg-white px-4 py-3"><select name="status" class="rounded-xl border bg-white px-3"><option value="">همه وضعیت‌ها</option>@foreach(['requested'=>'درخواست','assessed'=>'ارزیابی','approved'=>'تأیید','active'=>'در اجرا','closed'=>'بسته','rejected'=>'رد'] as $k=>$v)<option value="{{ $k }}" @selected(request('status')===$k)>{{ $v }}</option>@endforeach</select><button class="rounded-xl bg-slate-900 px-5 text-white">فیلتر</button></form><div class="overflow-hidden rounded-2xl border bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[800px] text-right text-sm"><thead class="bg-slate-50"><tr><th class="p-4">مجوز</th><th>نوع</th><th>واحد</th><th>درخواست‌کننده</th><th>بازه اجرا</th><th>وضعیت</th></tr></thead><tbody class="divide-y">@forelse($permits as $p)<tr><td class="p-4"><a class="font-bold hover:text-emerald-600" href="{{ route('permits.show',$p) }}">{{ $p->title }}</a><small class="block text-slate-400">{{ $p->code }}</small></td><td>{{ $p->type }}</td><td>{{ $p->department->name }}</td><td>{{ $p->requester->name }}</td><td>@jdatetime($p->starts_at)<small class="block text-slate-400">تا @jdatetime($p->ends_at)</small></td><td><x-status :status="$p->status" /></td></tr>@empty<tr><td colspan="6" class="p-12 text-center text-slate-400">مجوزی ثبت نشده است.</td></tr>@endforelse</tbody></table></div><div class="border-t p-4">{{ $permits->links() }}</div></div>@endsection
