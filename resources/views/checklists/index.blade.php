@extends('layouts.app') @section('title','چک‌لیست‌ها') @section('page-title','بانک چک‌لیست‌های بازرسی') @section('content')
<div class="mb-5 flex items-center justify-between"><div><h2 class="text-xl font-black">الگوهای استاندارد بازرسی</h2><p class="mt-1 text-sm text-slate-500">نسخه‌بندی و مدیریت سؤالات بازرسی</p></div><a href="{{ route('checklists.create') }}" class="rounded-xl bg-emerald-600 px-4 py-3 text-sm font-bold text-white">+ چک‌لیست جدید</a></div><div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@foreach($checklists as $c)<div class="rounded-2xl border border-slate-200 bg-white p-5"><div class="flex justify-between"><span class="rounded-lg bg-slate-100 px-2 py-1 text-[11px] font-bold">{{ $c->category }}</span><span class="h-2.5 w-2.5 rounded-full {{ $c->is_active?'bg-emerald-500':'bg-slate-300' }}"></span></div><h3 class="mt-4 font-black">{{ $c->title }}</h3><p class="mt-2 line-clamp-2 text-sm text-slate-500">{{ $c->description }}</p><div class="mt-5 flex items-center justify-between border-t pt-4 text-xs"><span>{{ $c->items_count }} سؤال · نسخه {{ $c->version }}</span><form method="post" action="{{ route('checklists.toggle',$c) }}">@csrf @method('PATCH')<button class="font-bold {{ $c->is_active?'text-rose-600':'text-emerald-600' }}">{{ $c->is_active?'غیرفعال‌کردن':'فعال‌کردن' }}</button></form></div></div>@endforeach</div><div class="mt-5">{{ $checklists->links() }}</div>@endsection
