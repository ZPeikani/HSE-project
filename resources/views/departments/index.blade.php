@extends('layouts.app') @section('title','واحدها') @section('page-title','ساختار سازمانی') @section('content')
<div class="grid gap-6 lg:grid-cols-[1fr_360px]"><section><h2 class="mb-4 text-xl font-black">واحدهای سازمان</h2><div class="grid gap-4 md:grid-cols-2">@foreach($departments as $d)<div class="rounded-2xl border border-slate-200 bg-white p-5"><div class="flex justify-between"><div><h3 class="font-black">{{ $d->name }}</h3><div class="text-xs text-slate-400">{{ $d->code }}</div></div><span class="rounded-full px-2 py-1 text-xs {{ $d->is_active?'bg-emerald-100 text-emerald-700':'bg-slate-100' }}">{{ $d->is_active?'فعال':'غیرفعال' }}</span></div><div class="mt-4 grid grid-cols-2 gap-3 text-xs"><div class="rounded-xl bg-slate-50 p-3"><span class="text-slate-400">مدیر واحد</span><b class="mt-1 block">{{ $d->manager?->name ?? 'تعیین نشده' }}</b></div><div class="rounded-xl bg-slate-50 p-3"><span class="text-slate-400">تعداد کاربران</span><b class="mt-1 block">{{ $d->users_count }} نفر</b></div></div><div class="mt-3 text-xs text-slate-500">{{ $d->location ?: 'محل ثبت نشده' }}</div></div>@endforeach</div><div class="mt-4">{{ $departments->links() }}</div></section><aside><form method="post" action="{{ route('departments.store') }}" class="sticky top-24 rounded-2xl border border-slate-200 bg-white p-6">@csrf<h3 class="font-black">افزودن واحد جدید</h3><div class="mt-5 space-y-4">@include('components.field',['label'=>'نام واحد','name'=>'name']) @include('components.field',['label'=>'کد واحد','name'=>'code']) @include('components.field',['label'=>'محل / ساختمان','name'=>'location','optional'=>true])<label><span class="mb-2 block text-sm font-bold">مدیر واحد</span><select name="manager_id" class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"><option value="">انتخاب نشده</option>@foreach($managers as $m)<option value="{{ $m->id }}">{{ $m->name }}</option>@endforeach</select></label><button class="w-full rounded-xl bg-emerald-600 px-5 py-3 text-sm font-black text-white">ثبت واحد</button></div></form></aside></div>@endsection
