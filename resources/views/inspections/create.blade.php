@extends('layouts.app') @section('title','بازرسی جدید') @section('page-title','برنامه‌ریزی بازرسی') @section('content')
<form method="post" action="{{ route('inspections.store') }}" class="mx-auto max-w-4xl">@csrf<div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="mb-6 border-b border-slate-100 pb-4"><h2 class="font-black">مشخصات بازرسی</h2><p class="mt-1 text-xs text-slate-400">چک‌لیست، واحد و بازرس مسئول را مشخص کنید.</p></div><div class="grid gap-5 md:grid-cols-2">
@include('components.field',['label'=>'عنوان بازرسی','name'=>'title','placeholder'=>'مثلاً بازرسی دوره‌ای کارگاه تولید'])
<label class="block"><span class="mb-2 block text-sm font-bold">چک‌لیست *</span><select name="checklist_id" required class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"><option value="">انتخاب کنید</option>@foreach($checklists as $c)<option value="{{ $c->id }}">{{ $c->title }} — نسخه {{ $c->version }}</option>@endforeach</select></label>
<label class="block"><span class="mb-2 block text-sm font-bold">واحد سازمانی *</span><select name="department_id" required class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"><option value="">انتخاب کنید</option>@foreach($departments as $d)<option value="{{ $d->id }}">{{ $d->name }}</option>@endforeach</select></label>
<label class="block"><span class="mb-2 block text-sm font-bold">بازرس مسئول *</span><select name="inspector_id" required class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"><option value="">انتخاب کنید</option>@foreach($inspectors as $u)<option value="{{ $u->id }}">{{ $u->name }}</option>@endforeach</select></label>
@include('components.field',['label'=>'تاریخ و ساعت برنامه','name'=>'scheduled_at','type'=>'datetime-local']) @include('components.field',['label'=>'محل دقیق بازرسی','name'=>'location','placeholder'=>'ساختمان / سالن / خط تولید'])
</div></div><div class="mt-5 flex justify-end gap-3"><a href="{{ route('inspections.index') }}" class="rounded-xl border border-slate-200 px-5 py-3 text-sm font-bold">انصراف</a><button class="rounded-xl bg-emerald-600 px-6 py-3 text-sm font-black text-white">ثبت برنامه بازرسی</button></div></form>@endsection
