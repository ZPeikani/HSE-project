@extends('layouts.app') @section('title','رویدادها') @section('page-title','حوادث و شبه‌حوادث') @section('content')
<div class="mb-5 flex items-center justify-between"><div><h2 class="text-xl font-black">گزارش رویدادهای HSE</h2><p class="mt-1 text-sm text-slate-500">ثبت سریع، بررسی علت و جلوگیری از تکرار رویدادها</p></div><a href="{{ route('incidents.create') }}" class="rounded-xl bg-rose-600 px-4 py-3 text-sm font-bold text-white hover:bg-rose-700">+ گزارش رویداد</a></div><div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($incidents as $i)<a href="{{ route('incidents.show',$i) }}" class="rounded-2xl border border-slate-200 bg-white p-5 transition hover:-translate-y-0.5 hover:shadow-lg"><div class="flex items-start justify-between"><span class="rounded-lg {{ $i->type==='near_miss'?'bg-amber-100 text-amber-700':'bg-rose-100 text-rose-700' }} px-2.5 py-1 text-[11px] font-bold">{{ ['incident'=>'حادثه','near_miss'=>'شبه‌حادثه','occupational_disease'=>'بیماری شغلی','environmental'=>'رویداد محیط‌زیستی'][$i->type] }}</span><x-status :status="$i->status" /></div><h3 class="mt-4 font-black">{{ $i->title }}</h3><p class="mt-2 line-clamp-2 text-sm leading-6 text-slate-500">{{ $i->description }}</p><div class="mt-5 flex items-center justify-between border-t border-slate-100 pt-4 text-xs text-slate-400"><span>{{ $i->department->name }}</span><span>@jdatetime($i->occurred_at)</span></div></a>@empty<div class="col-span-full rounded-2xl border border-dashed p-12 text-center text-slate-400">هنوز رویدادی گزارش نشده است.</div>@endforelse</div><div class="mt-5">{{ $incidents->links() }}</div>@endsection
