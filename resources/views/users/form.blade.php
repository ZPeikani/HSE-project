@extends('layouts.app') @section('title',$user->exists?'ویرایش کاربر':'کاربر جدید') @section('page-title',$user->exists?'ویرایش کاربر':'ایجاد کاربر') @section('content')
<form method="post" action="{{ $user->exists?route('users.update',$user):route('users.store') }}" class="mx-auto max-w-4xl rounded-2xl border border-slate-200 bg-white p-6">@csrf @if($user->exists)@method('PUT')@endif<div class="grid gap-5 md:grid-cols-2">@include('components.field',['label'=>'نام و نام خانوادگی','name'=>'name','value'=>$user->name]) @include('components.field',['label'=>'کد پرسنلی','name'=>'personnel_code','value'=>$user->personnel_code,'optional'=>true]) @include('components.field',['label'=>'ایمیل','name'=>'email','type'=>'email','value'=>$user->email]) @include('components.field',['label'=>'شماره تماس','name'=>'phone','value'=>$user->phone,'optional'=>true]) @include('components.field',['label'=>$user->exists?'رمز جدید (اختیاری)':'رمز عبور','name'=>'password','type'=>'password','optional'=>$user->exists])<label><span class="mb-2 block text-sm font-bold">نقش *</span><select name="role" required class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3">@foreach($roles as $role)<option value="{{ $role->value }}" {{ $user->role===$role?'selected':'' }}>{{ $role->label() }}</option>@endforeach</select></label><label><span class="mb-2 block text-sm font-bold">واحد سازمانی</span><select name="department_id" class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"><option value="">بدون واحد</option>@foreach($departments as $d)<option value="{{ $d->id }}" {{ $user->department_id===$d->id?'selected':'' }}>{{ $d->name }}</option>@endforeach</select></label><label class="flex items-center gap-3 pt-8"><input type="hidden" name="is_active" value="0"><input type="checkbox" name="is_active" value="1" {{ !$user->exists||$user->is_active?'checked':'' }} class="h-5 w-5 rounded text-emerald-600"><span class="text-sm font-bold">حساب فعال باشد</span></label></div><div class="mt-6 flex justify-end"><button class="rounded-xl bg-emerald-600 px-7 py-3 text-sm font-black text-white">ذخیره کاربر</button></div></form>@endsection
