@extends('layouts.app') @section('title','کاربران') @section('page-title','کاربران و دسترسی‌ها') @section('content')
<div class="mb-5 flex items-center justify-between"><div><h2 class="text-xl font-black">حساب‌های سازمانی</h2><p class="mt-1 text-sm text-slate-500">مدیریت نقش، واحد و وضعیت دسترسی کاربران</p></div><a href="{{ route('users.create') }}" class="rounded-xl bg-emerald-600 px-4 py-3 text-sm font-bold text-white">+ کاربر جدید</a></div><div class="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[800px] text-right text-sm"><thead class="bg-slate-50 text-xs text-slate-500"><tr><th class="p-4">کاربر</th><th>نقش</th><th>واحد</th><th>تماس</th><th>وضعیت</th><th></th></tr></thead><tbody class="divide-y">@foreach($users as $u)<tr><td class="p-4"><div class="flex items-center gap-3"><div class="grid h-10 w-10 place-items-center rounded-xl bg-emerald-100 font-black text-emerald-700">{{ mb_substr($u->name,0,1) }}</div><div><b>{{ $u->name }}</b><div class="text-[11px] text-slate-400">{{ $u->personnel_code }}</div></div></div></td><td>{{ $u->role->label() }}</td><td>{{ $u->department?->name ?? '—' }}</td><td><div>{{ $u->email }}</div><div class="text-xs text-slate-400">{{ $u->phone }}</div></td><td><span class="rounded-full px-2 py-1 text-xs font-bold {{ $u->is_active?'bg-emerald-100 text-emerald-700':'bg-slate-100 text-slate-500' }}">{{ $u->is_active?'فعال':'غیرفعال' }}</span></td><td><div class="flex gap-2"><a href="{{ route('users.edit',$u) }}" class="font-bold text-sky-600">ویرایش</a>@if(!$u->is(auth()->user()))<form method="post" action="{{ route('users.toggle',$u) }}">@csrf @method('PATCH')<button class="font-bold text-rose-600">تغییر وضعیت</button></form>@endif</div></td></tr>@endforeach</tbody></table></div><div class="border-t p-4">{{ $users->links() }}</div></div>@endsection
