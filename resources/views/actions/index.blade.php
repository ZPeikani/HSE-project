@extends('layouts.app') @section('title','اقدامات اصلاحی') @section('page-title','اقدامات اصلاحی') @section('content')
<div class="mb-5 flex items-center justify-between"><div><h2 class="text-xl font-black">پیگیری اقدامات</h2><p class="mt-1 text-sm text-slate-500">از ارجاع تا اجرا و تأیید اثربخشی</p></div>@if(auth()->user()->hasRole(['admin','hse_manager']))<a href="{{ route('actions.create') }}" class="rounded-xl bg-emerald-600 px-4 py-3 text-sm font-bold text-white">+ اقدام جدید</a>@endif</div><div class="mb-4 flex flex-wrap gap-2">@foreach([''=>'همه','assigned'=>'ارجاع‌شده','pending_verification'=>'منتظر تأیید','rejected'=>'عودت‌شده','verified'=>'تأییدشده'] as $v=>$l)<a href="{{ route('actions.index',['status'=>$v]) }}" class="rounded-xl border px-4 py-2 text-xs font-bold {{ request('status','')===$v?'border-slate-900 bg-slate-900 text-white':'border-slate-200 bg-white' }}">{{ $l }}</a>@endforeach</div><div class="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[850px] text-right text-sm"><thead class="bg-slate-50 text-xs text-slate-500"><tr><th class="p-4">اقدام</th><th>واحد</th><th>مسئول</th><th>اولویت</th><th>سررسید</th><th>وضعیت</th></tr></thead><tbody class="divide-y">@forelse($actions as $a)<tr class="{{ $a->is_overdue?'bg-rose-50/50':'' }}"><td class="p-4"><a href="{{ route('actions.show',$a) }}" class="font-bold hover:text-emerald-600">{{ $a->title }}</a><div class="text-[11px] text-slate-400">{{ $a->code }}</div></td><td>{{ $a->department->name }}</td><td>{{ $a->assignee->name }}</td><td>{{ ['low'=>'کم','medium'=>'متوسط','high'=>'زیاد','critical'=>'فوری'][$a->priority] }}</td><td class="{{ $a->is_overdue?'font-black text-rose-600':'' }}">@jdate($a->due_date) @if($a->is_overdue)<small>(گذشته)</small>@endif</td><td><x-status :status="$a->status" /></td></tr>@empty<tr><td colspan="6" class="p-12 text-center text-slate-400">اقدامی یافت نشد.</td></tr>@endforelse</tbody></table></div><div class="border-t p-4">{{ $actions->links() }}</div></div>@endsection
