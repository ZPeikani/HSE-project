@extends('layouts.app') @section('title','اقدام جدید') @section('page-title','تعریف اقدام اصلاحی') @section('content')
<form method="post" action="{{ route('actions.store') }}" class="mx-auto max-w-4xl rounded-2xl border border-slate-200 bg-white p-6">@csrf @if($sourceType)<input type="hidden" name="source_type" value="{{ $sourceType }}"><input type="hidden" name="source_id" value="{{ $sourceId }}">@endif<div class="mb-6"><h2 class="font-black">مشخصات اقدام</h2><p class="text-xs text-slate-400">اقدام باید مشخص، قابل سنجش، دارای مسئول و زمان‌بندی روشن باشد.</p></div><div class="grid gap-5 md:grid-cols-2">@include('components.field',['label'=>'عنوان اقدام','name'=>'title'])<label><span class="mb-2 block text-sm font-bold">واحد مسئول *</span><select name="department_id" required class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"><option value="">انتخاب کنید</option>@foreach($departments as $d)<option value="{{ $d->id }}">{{ $d->name }}</option>@endforeach</select></label><label class="md:col-span-2"><span class="mb-2 block text-sm font-bold">شرح دقیق اقدام و معیار پذیرش *</span><textarea name="description" required rows="4" class="w-full rounded-xl border border-slate-200 bg-slate-50 p-4">{{ old('description') }}</textarea></label><label><span class="mb-2 block text-sm font-bold">مسئول اجرا *</span><select name="assignee_id" required class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"><option value="">انتخاب کنید</option>@foreach($users as $u)<option value="{{ $u->id }}">{{ $u->name }} — {{ $u->department?->name }}</option>@endforeach</select></label><label><span class="mb-2 block text-sm font-bold">اولویت *</span><select name="priority" class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"><option value="medium">متوسط</option><option value="high">زیاد</option><option value="critical">فوری / بحرانی</option><option value="low">کم</option></select></label>@include('components.field',['label'=>'تاریخ سررسید','name'=>'due_date','type'=>'date'])</div><div class="mt-6 flex justify-end"><button class="rounded-xl bg-emerald-600 px-7 py-3 text-sm font-black text-white hover:bg-emerald-700">ارجاع اقدام</button></div></form>@endsection
