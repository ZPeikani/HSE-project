@extends('layouts.app') @section('title','ریسک‌ها') @section('page-title','خطرات و ارزیابی ریسک') @section('content')
<div class="mb-5 flex items-center justify-between"><div><h2 class="text-xl font-black">دفتر ثبت ریسک</h2><p class="mt-1 text-sm text-slate-500">شناسایی، امتیازدهی و پیگیری کنترل خطرات</p></div><a href="{{ route('risks.create') }}" class="rounded-xl bg-emerald-600 px-4 py-3 text-sm font-bold text-white hover:bg-emerald-700">+ ثبت خطر جدید</a></div>
<div class="mb-4 flex flex-wrap gap-2">@foreach([''=>'همه','بحرانی'=>'بحرانی','زیاد'=>'زیاد','متوسط'=>'متوسط','کم'=>'کم'] as $v=>$l)<a href="{{ route('risks.index',['level'=>$v]) }}" class="rounded-xl border px-4 py-2 text-xs font-bold {{ request('level','')===$v?'border-emerald-600 bg-emerald-600 text-white':'border-slate-200 bg-white' }}">{{ $l }}</a>@endforeach</div>
<div class="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[800px] text-right text-sm"><thead class="bg-slate-50 text-xs text-slate-500"><tr><th class="p-4">خطر</th><th>دسته‌بندی</th><th>واحد</th><th>احتمال × شدت</th><th>امتیاز / سطح</th><th>وضعیت</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($risks as $r)<tr><td class="p-4"><a class="font-bold hover:text-emerald-600" href="{{ route('risks.show',$r) }}">{{ $r->title }}</a><div class="text-[11px] text-slate-400">{{ $r->code }}</div></td><td>{{ $r->category }}</td><td>{{ $r->department->name }}</td><td>{{ $r->likelihood }} × {{ $r->severity }}</td><td><span class="inline-grid h-9 w-9 place-items-center rounded-xl {{ $r->risk_score>=17?'bg-rose-100 text-rose-700':($r->risk_score>=10?'bg-orange-100 text-orange-700':($r->risk_score>=5?'bg-amber-100 text-amber-700':'bg-emerald-100 text-emerald-700')) }} font-black">{{ $r->risk_score }}</span> <b class="mr-2">{{ $r->risk_level }}</b></td><td><x-status :status="$r->status" /></td></tr>@empty<tr><td colspan="6" class="p-12 text-center text-slate-400">ریسکی ثبت نشده است.</td></tr>@endforelse</tbody></table></div><div class="border-t p-4">{{ $risks->links() }}</div></div>@endsection
