# گزارش توسعه افزایشی ماژول ایمنی HSE

## نتیجه ممیزی و Gap Analysis

| قابلیت | وضعیت اولیه | اقدام انجام‌شده |
|---|---|---|
| Risk و ماتریس ۵×۵ | موجود و قابل استفاده | حفظ و تکمیل با ریسک اولیه/باقیمانده، مسئول و بازنگری |
| JHA/JSA | موجود نبود | به Risk فعلی متصل شد؛ مراحل، خطر، پیامد، کنترل و امتیازها |
| FMEA | موجود نبود | به Risk فعلی متصل شد؛ S/O/D و RPN خودکار و تولید CAPA |
| Checklist و Inspection | موجود ولی ناقص | حفظ و تکمیل با مستند، عدم‌انطباق، تولید Risk/CAPA و امتیاز واقعی |
| Incident / Near Miss | موجود ولی ناقص | تکمیل اطلاعات افراد، فعالیت، پیامد و مستندات |
| RCA | فقط متن علت ریشه‌ای | ۵ چرا، Fishbone، Fault Tree و حالت ترکیبی به Incident اضافه شد |
| CAPA | موجود ولی گردش کار ناقص | حفظ و توسعه با شروع، اجرا، انتظار تأیید، اثربخشی، عودت و بستن |
| Alarm / Notification | موجود نبود | زیرساخت عمومی اعلان و سه حالت قبل/روز/پس از سررسید |
| PPE | موجود نبود | نوع، نیاز شغلی، تحویل، تعویض و سوابق |
| تجهیزات ایمنی و حریق | موجود نبود | شناسه، محل، بازرسی، سرویس/شارژ، انقضا و سررسید |
| PTW | موجود نبود | درخواست، ارزیابی، تأیید، اجرا، رد و بستن |
| Dashboard/KPI | موجود ولی محدود | KPI واقعی تجهیزات، PPE، PTW، CAPA، Risk و Incident |
| گزارش | موجود ولی محدود | حفظ گزارش فعلی، چاپ/PDF مرورگر و خروجی CSV سازگار با Excel |
| دسترسی و تفکیک واحد | موجود | روی ماژول‌های جدید و عملیات حساس در Backend اعمال شد |

## اصول حفظ‌شده

- پروژه، Laravel 12، Tailwind 4، Routeها، Layout و Design System فعلی حفظ شدند.
- هیچ قابلیت سالم، جدول یا داده فعلی حذف نشده است.
- برای مفاهیم Risk، Incident، Inspection و Corrective Action مدل موازی ساخته نشد.
- هیچ Migration جدیدی ایجاد نشد. تغییر دیتابیس فقط در `DATABASE_UPDATE_HSE_INCREMENTAL.sql` است.
- همه جدول‌های جدید فقط برای موجودیت‌های واقعاً جدید ساخته شده‌اند.
- فرم‌ها اعتبارسنجی Backend و عملیات حساس کنترل دسترسی Backend دارند.

## ترتیب نصب دیتابیس

1. از دیتابیس فعلی بکاپ کامل بگیرید.
2. فایل `DATABASE_UPDATE_HSE_INCREMENTAL.sql` را روی همان دیتابیس Import کنید.
3. دستورهای `php artisan optimize:clear` و `npm run build` را اجرا کنید.
4. مسیرها و نقش‌ها را طبق بخش تست پذیرش بررسی کنید.

## تست پذیرش پیشنهادی

1. ثبت Risk با ماتریس ۵×۵، ریسک باقیمانده و تاریخ بازنگری.
2. افزودن JHA/JSA و FMEA از صفحه همان Risk و کنترل RPN.
3. تکمیل Inspection با عدم‌انطباق، فایل و گزینه‌های ایجاد Risk/CAPA.
4. ثبت Incident و سپس RCA با ۵ چرا/Fishbone/Fault Tree.
5. اجرای کامل CAPA از ارجاع تا بستن و مشاهده اعلان سررسید.
6. تعریف نوع PPE، نیاز شغلی و تحویل به کاربر.
7. ثبت تجهیز حریق و ثبت بازرسی/سرویس.
8. اجرای گردش PTW از درخواست تا بستن.
9. کنترل محدودیت واحد برای مدیر واحد و محدودیت عملیات مدیریتی برای بازرس.
10. بررسی داشبورد، چاپ/PDF و خروجی CSV با داده واقعی.

## فایل‌های اصلی تغییرکرده یا اضافه‌شده

- مدل‌های موجود Risk، Incident، InspectionResponse، CorrectiveAction، Department و User تکمیل شدند.
- کنترلرهای Risk، Inspection، Incident، CorrectiveAction، Dashboard و Report تکمیل شدند.
- کنترلرها و مدل‌های JHA/JSA، FMEA، Notification، PPE، Safety Equipment و Work Permit اضافه شدند.
- Routeها، Sidebar، منوی موبایل، Dashboard، فرم‌ها و صفحات جزئیات با الگوی RTL فعلی تکمیل شدند.
- `DATABASE_UPDATE_HSE_INCREMENTAL.sql` تنها فایل تغییر ساختار MySQL است.
